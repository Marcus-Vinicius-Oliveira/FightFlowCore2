import { AuthTabs } from '../AuthForms';

export default function AuthFormsExample() {
  return (
    <div className="p-8 min-h-screen flex items-center justify-center bg-muted/50">
      <AuthTabs />
    </div>
  );
}